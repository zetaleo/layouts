(function ($) {
  $(document).ready(() => {
    const $hamburger = $(".hamburger");
    $hamburger.on("click", () => {
      $(".destkop-menu").toggleClass("display-none");
      $(".mobile-menu").toggleClass("is-open");
      $hamburger.toggleClass("is-active");
    });
  });
})(jQuery);
